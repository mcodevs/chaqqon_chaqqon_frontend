# Copy to .env.local. Leave both empty to run entirely in the browser (localStorage).
# Supabase dashboard → Project Settings → API keys: Project URL and the publishable key
# (the legacy anon key works too). Never put the service-role / secret key here.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
